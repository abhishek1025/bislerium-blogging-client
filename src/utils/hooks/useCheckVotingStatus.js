import { useQuery } from '@tanstack/react-query';
import { getRequest } from '../api';
import useUserAuthContext from './useUserAuthContext';

const useCheckVotingStatus = ({ endpoint }) => {
  return;
};

export default useCheckVotingStatus;

